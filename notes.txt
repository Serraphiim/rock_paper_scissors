next steps
add a couple pictures picture to show what each side chose
add an array that stores the results of each roudn and display them


styling fixes
need to add max width and heights

add rock paper sissors delay to the code on each role

cssImages branch is to change the images to css images instead of html tthen to us document queries to display images in a variable
